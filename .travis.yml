language: android

before_install:
 - chmod +x gradlew

android:
  components:
    - tools # Update preinstalled tools
    - build-tools-26.0.2
    - platform-tools
    - tools # Update tools again

    # The SDK version used to compile your project
    - android-26

jdk: oraclejdk8